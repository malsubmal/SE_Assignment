
mod model;
mod customer;
mod branch;
mod car;
mod entity;
mod rentalgroup;
mod usecase;
mod usecases;
mod test;
mod carreturn;
mod mockdatabase;
mod cpsystem;

fn main() {


    /* let mut db = HashMap::new();
    let c1 = Customer::new(String::from("A"), String::from("012"));
    let c2 = Customer::new(String::from("A"), String::from("013"));
    db.insert(c1.getqueryfield().to_string(),c1);
    db.insert(c2.getqueryfield().to_string(),c2);


    let uc_createCustomer = |customer| Usecase::new(
        &|| db.CustomerShouldNotExist(customer_id.to_string()),
        &|| db.insertCustomer(customer),
        &|| db.CustomerShouldExist(customer_id.to_string())
    ).run(); */
    
}



